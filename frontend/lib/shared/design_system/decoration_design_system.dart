import 'package:flutter/material.dart';

/// Used for properties such as the default and only border radius of MyoroPlayer
final class DecorationDesignSystem {
  static final borderRadius = BorderRadius.circular(10);
}
