import 'package:flutter/material.dart';

class DecorationDesignSystem {
  static BorderRadius get borderRadius => BorderRadius.circular(10);
  static Duration get tooltipWaitDuration => const Duration(milliseconds: 200);
}
